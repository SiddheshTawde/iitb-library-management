import { Header } from "./header";
import { RenderBookList } from "./list";
import { Navbar } from "./navbar";

const core = {
  header: Header,
  nav: Navbar,
  list: RenderBookList,
};

export { core };
